# The public repository contains framework defaults only.
# Copy this file to .env for a local deployment if/when env loading is added.
#
# Never commit real device names, tokens, tunnel domains, project paths,
# workspace bindings, private IP addresses, or credentials.

CODASSOL_LISTEN_HOST=127.0.0.1
CODASSOL_LISTEN_PORT=17676
CODASSOL_BACKEND_HOST=127.0.0.1
CODASSOL_BACKEND_PORT=7676
CODASSOL_BACKEND_TIMEOUT_MS=15000

